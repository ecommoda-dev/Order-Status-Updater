<div dir="rtl" style="text-align: right;">

# محدّث حالة الأوردر (`Order-Status-Updater`)

![version](https://img.shields.io/badge/version-v1.3.0-blue)

**بتعمل إيه:** موظف العمليات بيدوّر على أوردرات (رقم · مندوب · اسم) وبيحدّث
مرحلة الأوردر — دورة S1 (`custom.manual_status`) ودورة الإرجاع/الاستبدال S2
(`custom.status_2_r_e`) — مع المندوب وتاريخ الاستلام وسبب الإلغاء/الإرجاع.
**مين بيستخدمها:** مخزن · عمليات
**الإصدار:** Worker `v4.7.0` · الواجهة `v4.6.0`   ← الاتنين مستقلين، طبيعي يختلفوا

## 🔗 الأداة دي مدموجة تحت مركز الشحن والتحصيل (16-09-2026)

**قرار أحمد 16-09-2026:** الأداة دي بقت **صفحة جوّه**
`Delivery-COD-Operations-Center` — `order-status.html` — بدخول واحد وسر واحد
للمركز كله.

```
نسخة المركز : https://ecommoda-dev.github.io/Delivery-COD-Operations-Center/order-status.html
النسخة دي   : https://ecommoda-dev.github.io/Order-Status-Updater/          ← 🔴 **لسه شغّالة**
```

> ✅ **والـ Worker ما اتلمسش ولا سطر.** `order-status-updater-worker` لسه
> واحد في الريبو ده، و**النسختين بينادوا نفس الـ Worker**: أي تحديث حالة من
> أي نسخة بيتسجّل في **نفس** صفوف D1 بنفس `tool='order_status'`.
> ⛔ **يعني `§CONTRACT::extra` و`TOOL_NAME` ما اتغيّرش فيهم حرف**، و
> `cod-payment-center-worker` اللي بيقرا الصفوف دي **مالوش أي أثر** من الدمج.

### 🔴 المطلوب منك هنا — تدوير السر

`WORKER_SECRET` على `order-status-updater-worker` لازم يتدوّر لقيمة مجموعة
**`delivery_cod_ops`** (قرار أحمد ②: الـ Workers تنضم للمجموعة بدل ما المركز
يخزّن تلات أسرار).

- ⛔ **قبل التدوير:** صفحة المركز بترجّع `401` على كل نداء.
- ⚠️ **وبعد التدوير:** **النسخة دي** كمان محتاجة القيمة الجديدة — الموظف اللي
  بيفتح الرابط ده لازم يلزقها في ⚙️ الإعدادات. المفتاح لسه
  `order_status_worker_secret` هنا، ومفتاح المركز مختلف
  (`delivery_cod_ops_worker_secret`) — **قيمتين مخزّنتين ونفس السر**.
- 🔴 **ولازم تتسجّل في `ecommoda-constants` → `references/secret-groups.md`**
  (قاعدة ٢): الـ Worker ده بقى **عضو في مجموعة `delivery_cod_ops`**.
  عضو غير مسجّل = إجراء التدوير بيتكسر **بصمت**.

### 🔴 نسختان من الواجهة على الهوا — درس R1

أحمد قرر إن الرابط ده **يفضل شغّال زي ما هو** (مش تحويل للمركز). يعني فيه
**نسختين من نفس الواجهة** — وده بالظبط درس R1: «اتصلح في واحدة وفضل مكسور في
التانية لشهور».

- 🔴 **اللي بيقلّل الخطر:** نسخة المركز **متولّدة من الملف ده** مش متكتوبة:
  ```bash
  cd ../Delivery-COD-Operations-Center
  python3 docs/port-standalone.py && node docs/tools-check.mjs
  ```
- ⛔ **فأي تعديل في `index.html` هنا لازم يتبعه تشغيل المولّد ودفعة في ريبو
  المركز** — وإلا الإصلاح بيوصل لنص الموظفين بس.
- ✅ **والمولّد مربوط بمراسي نصّية**: لو تعديلك حرّك كتلة هو بيتعمّل بيها،
  السكربت **بيقف بصوت عالي** بدل ما يولّد صفحة ناقصة. ساعتها عدّل المرساة في
  `docs/port-standalone.py` بعد ما تتأكد إن الكتلة لسه هي هي.
- ⚠️ **والاتجاه واحد**: تعديل يدوي في `order-status.html` بتاع المركز **بيضيع**،
  وفيه بند في `docs/tools-check.mjs` بيقارن البايتات وبيمسكه.

### اللي نسخة المركز بتعمله مختلف

| هنا | في المركز |
|---|---|
| شاشة دخول بالـ PIN + `get_employees` | `requireSession()` — الدخول مرة واحدة في المركز |
| `LS_SECRET = 'order_status_worker_secret'` | سر مجموعة `delivery_cod_ops` |
| `TOOL_VERSION = '4.6.0'` في الهيدر | نسخة الهب (`v1.5.0`) — وبند النسخة دي في سجل التحديثات |
| كتلة توكنز ٤٧ متغيّر | `shared/shell.css` |
| صف `login` في D1 باسم `order_status` | صف `login` باسم `delivery_cod_ops_center` |

> ✅ **ومنطق الأداة نفسه واحد بالحرف** — نفس الخطوات ونفس `update_status` ونفس
> المانفيست ونفس السجل. صفوف `update` في D1 **متطابقة** من النسختين.
> ⚠️ **وعدّاد `login` بتاع `order_status` بيقف عند رقمه** بقدر ما الناس بتستخدم
> نسخة المركز — مش عطل، بس أي استعلام بيعدّ دخول الأداة دي لازم يعرف.

## الروابط

```
الواجهة    : https://ecommoda-dev.github.io/Order-Status-Updater/
الـ Worker : https://order-status-updater-worker.ecommoda-dev.workers.dev
اسم الـ Worker في الداشبورد: order-status-updater-worker   ← لازم يطابق name في wrangler.toml
```

## الـ Endpoints

| `?action=` | بيعمل إيه |
|---|---|
| `check_employee` · `register_pin` · `verify_employee` · `log_logout` | دورة دخول الموظف |
| `get_employees` · `get_config` | بيانات مساعدة للواجهة |
| `resolve_orders` · `resolve_by_name` · `search_courier_orders` | البحث عن أوردرات |
| `order_statuses` · `courier_values` · `reason_values` | قراءة حالة وقيم |
| `order_details` | بيانات المانفيست — عميل + **`cod` موقّع** + **`orderType`** + `missing[]` |
| `update_status` | **الكتابة** — تحديث S1/S2 + المندوب + التاريخ + السبب |
| `diag` | فحص ذاتي بدون أي كتابة — ممنوع يرجّع قيمة أي سر |
| `get_logs` · `get_logs_count` · `get_logs_export` | سجل العمليات (بفلاتر) |

## D1

```
tool  : order_status
type  : update · login · logout
```

مسجّلة في `ecommoda-constants` §7 ✅ — مفيش قيمة جديدة اتضافت في النقل ده.
الأداة بتستخدم `logs` و`employees` المشتركين بس — مفيش جداول إضافية.

### 🔗 عقد عابر للأدوات — `extra` مقروء من أداة تانية

من 31-08-2026، **`cod-payment-center-worker`** بيقرا صفوف الأداة دي من D1
**مباشرة** (مش نداء Worker-to-Worker) وبيعتمد حرفيًا على:
`tool='order_status'` · `type ≠ login/logout` · `order_id` ·
`extra.result ∈ ('success','warning')` · `extra.courier` · `extra.targetLabel`
(مع fallback على `extra.specifier`) · `timestamp`.

🔴 تغيير اسم أو قيمة أي مفتاح من دول — أو قيمة `TOOL_NAME` — **بيكسر أداة
التحصيل في صمت تام** (ليستة فاضية بلا خطأ، والموظف بيقفل من غير ما يحصّل).
قبل أي تعديل في `§CONTRACT::extra` أو `TOOL_NAME`: افتح `§TODAY-IMPORT` في
`cod-payment-center-worker` وعدّله في **نفس التسليم**.

### حقول السبب (v4.3.0)

| الحقل | معناه |
|---|---|
| `reasonIsNew` (داخلي) | الموظف اختار سبب **جديد** مختلف عن المسجّل على الأوردر — الكتابة على الميتافيلد مشروطة بيه |
| `extra.reasonSource` | `'operator'` = الموظف اختاره من الأداة · `'existing'` = كان مسجّل على شوبيفاي (خدمة العملاء) والأداة ماكتبتش فوقه · `null` = مفيش سبب |

### `extra.cancelVerify` (v4.4.0)

مفتاح **مضاف** بيتكتب على الصفوف اللي الإلغاء فيها ما اتأكدش بس
(`{ jobDone, attempts, waitedMs }`) — تشخيص انتظار الـ `orderCancel` job.

✅ **مالوش أي أثر على العقد العابر للأدوات:** مفيش مفتاح قديم اتغيّر أو اتشال،
و`cod-payment-center-worker` بيقرا `result` · `courier` · `targetLabel` ·
`specifier` بس — كلهم زي ما هم بالحرف. `§TODAY-IMPORT` **ما اتلمسش** ومش
محتاج يتعدّل مع التسليم ده.

القواعد التجارية لسبب تغيير الحالة → `ecommoda-order-lifecycle` §1.5.
أسماء الحقول دي مكانها هنا (قرار 26-08-2026).

### `extra.cancelVerify` وقيم `actions` المضافة (v4.5.0)

`actions` ممكن تحتوي دلوقتي على `orderCancel:skipped-already-cancelled` —
معناها إن الأوردر كان **ملغي على شوبيفاي قبل النداء**، فالأداة كتبت الحالة بس.
✅ مالهاش أثر على العقد العابر للأدوات: `cod-payment-center-worker` بيقرا
`result` · `courier` · `targetLabel` · `specifier` بس.

## قواعد الانتقال — قرارات محلية

> الجدول المرجعي في `ecommoda-order-lifecycle` → `state-machines.md` §1.4/§2.2.
> اللي هنا **إضافات أو استثناءات** خاصة بالأداة دي، متسجّلة عشان ما تتفتحش تاني.

| القاعدة | القرار | المصدر |
|---|---|---|
| `S1: In-Return → Returned` | ✅ مسموح (v4.5.0) | جدول `state-machines` §1.4 |
| `S2: In-Return → Returned` | ✅ مسموح (v4.5.0) | جدول `state-machines` §2.2 |
| `S2: In-Return → Ready` | ✅ مسموح (v4.5.0) — **قرار أحمد 08-09-2026**: القطعة بترجع للمكتب وممكن تطلع في دورة تسليم جديدة، أو العميل يلغي أصلاً | 🔴 **مش في المهارة لسه** |
| `S1: In-Return → Ready` | ⛔ **مش مسموح** — مش في الجدول المعتمد ولا اتقرر. سؤال مفتوح | — |
| `S1: Shipped → Cancelled` | ⛔ ممنوع من v3.6.0 | §1.2 (نافذتا الإلغاء) |

🔒 **قفل بند مفتوح:** الشرط الملزم في `state-machines.md` §2.3 (رفض
`Confirmed + RETURN` / `Confirmed + EXCHANGE` طول ما فيه دورة R/E مفتوحة)
**لا ينطبق على الأداة دي** — هي بتكتب في S2 القيم `Ready` · `Shipped` ·
`Returned` بس، وعمرها ما كتبت القيمتين دول. المهارة بتقول عنه «binding
requirement for the next piece of work on that Worker» — البند اتفحص في
مراجعة 08-09-2026 وطلع **غير منطبق**. متفتحوش تاني.

## إلغاء الـ Fulfillment عند `Shipped → Ready` — سلوك مقصود

الأداة **بتلغي** الـ fulfillment على شوبيفاي لما الأوردر يرجع من `Shipped`
لـ `Ready`. **ده مقصود ومعتمد (أحمد، 08-09-2026):** الأوردر بيرجع للمكتب
لحد ما تتعمل دورة تسليم جديدة، وممكن العميل يلغيه أصلاً — فلازم يرجع
Unfulfilled عشان `Ready → Cancelled` تفضل صالحة (§1.2: الإلغاء لازم يحصل
قبل ما الأوردر يبقى Fulfilled).

🔴 **`ecommoda-order-lifecycle` Rule 5 و §1.3 و §4 بيقولوا العكس** («الـ
fulfillment مابيتلغيش أبدًا في لوب إعادة التسليم»، و`Ready` + `Fulfilled`
تركيبة صالحة ومتوقعة). القياس في 08-09-2026: **٦٩** انتقال `Shipped → Ready`
ناجح، و**صفر** أوردر عليه `Ready` + `FULFILLED` في المتجر — يعني الحالة اللي
المهارة بتوصفها مش موجودة في الداتا خالص.

**النتيجة العملية:** أي عدّاد بيفرّق بين «شحنة أولى» و«محاولة مكررة**
**مايقدرش** يستخدم حالة الـ fulfillment — المصدر الوحيد هو **سجل D1**:

```sql
SELECT date(timestamp) d, COUNT(*) n FROM logs
WHERE tool='order_status' AND type='update'
  AND json_extract(extra,'$.targetLabel')='Ready'
  AND json_extract(extra,'$.s1Before')='Shipped'
  AND json_extract(extra,'$.result')='success'
GROUP BY d ORDER BY d DESC;
```

المهارة محتاجة تعديل — مسجّل في `skills-updates-2026-09-08.md`.

## 🔴 المانفيست — عمود COD وعمود «نوع الأوردر» (v4.7.0 · 16-09-2026)

`?action=order_details` هو المصدر الوحيد لصفوف المانفيست، والعمود `cod` كان
**بيطبع رقم مخترع على كل أوردر استبدال/استرجاع**. الصيغة القديمة:

```js
cod: Math.abs(outstanding > 0 ? outstanding : current).toFixed(2)
//                              ↑ currentTotalPriceSet
```

الفرع التاني كان بيشتغل **بس** لما `outstanding <= 0` — وده بالظبط **تعريف**
أوردر الـ R/E (مدفوع بالكامل، أو عليه استرداد). يعني الـ `fallback` ماكانش
بيغطّي حالة ناقصة، كان **مضمون يشتغل غلط**. و`Math.abs` كان بيمسح إشارة
الاسترداد فوقها.

**القياس الحي (16-09-2026 — أربع أوردرات من مانفيست `Sobhy`):**

| الأوردر | النوع | `totalOutstandingSet` | `currentTotalPriceSet` | اللي اتطبع | الصح |
|---|---|---|---|---|---|
| `#54869` | عادي | `2675` | `2675` | **2,675** ✅ | 2,675 |
| `#53987` | استبدال | `0` | `2950` | **2,950** 🔴 | **0** |
| `#54173` | استرجاع | `-2125` | `150` | **150** 🔴 | **−2,125** |
| `#53648` | استرجاع | `-1750` | `75` | **75** 🔴 | **−1,750** |

على الأربعة دول لوحدهم: الورقة كانت بتقول «حصّل 5,850» والحقيقة «حصّل 2,675 ·
ادفع 3,875 · الصافي −1,200» — **فرق 7,050 ج على ورقة واحدة**، وهي الورقة اللي
المندوب بيسوّي عليها آخر اليوم.

🔴 **والـ `#53987` أخطر من الرقم الغلط:** المندوب بيروح لعميل **دافع بالكامل**
ومعاه ورقة بتقوله يحصّل 2,950.

### القرار — `cod` = `totalOutstandingSet` موقّع وبس

`currentTotalPriceSet` **اتشال من الاستعلام خالص** — مستهلكه الوحيد كان الـ
fallback الغلط. والقيمة بقت موقّعة:

```
موجب = المندوب بيحصّل  ·  سالب = المندوب بيدفع للعميل  ·  صفر = مفيش حركة فلوس
null  = شوبيفاي ما رجّعتش الرقم  ← مش صفر
```

🔗 **ودي نفس القيمة بالحرف في تلات أدوات دلوقتي** — الاتساق ده هو المكسب
الحقيقي مش مجرد إصلاح رقم:

| الأداة | الحقل | المصدر |
|---|---|---|
| `cod-payment-center-worker` | `amount` (اللي بيتحصّل فعلاً) | `getOrderOutstanding` → `totalOutstandingSet` |
| `order-printer-worker` | `invoice.totalDue` (سطر Total في الفاتورة) | `totalOutstandingSet` |
| **`order-status-updater-worker`** | `details[].cod` (عمود COD في المانفيست) | `totalOutstandingSet` |

⚠️ **قبل الإصلاح كانت الورقة بتناقض نفسها:** فاتورة الـ R/E المطبوعة من
أداة الطباعة كانت بتقول `Total = −2,125` والمانفيست جنبها بيقول `150` —
**نفس الأوردر، نفس الرحلة، نفس المخزن**.

⚠️ **والرقم شامل الشحن عن قصد** — `courierCollect` في أداة التحصيل
(`outstanding − returnShipping`) هو «تحصيل الأوردر **بدون** الشحن» وله عمود
شحن منفصل جنبه. المانفيست بيوصف **حركة الفلوس عند الباب**، وهي `outstanding`
بالحرف.

### `orderType` — عادي · استبدال · استرجاع

```js
cycles = returns.nodes.filter(status ∉ {CANCELED, DECLINED}).sort(createdAt)
cycle  = cycles.at(-1)
!cycle ? 'normal' : (cycle.exchangeLineItems.nodes.length ? 'exchange' : 'return')
```

- ⛔ **ممنوع من `custom.status_2_r_e`.** قيمته وقت رحلة المندوب بتبقى
  `Ready`/`Shipped`/`Returned` — التفرقة بين `RETURN` و`EXCHANGE` **مش موجودة
  فيه أصلاً** في اللحظة اللي المانفيست بيتطبع فيها. (متأكَّد حيًا: التلات
  أوردرات R/E فوق كلهم `s2 = "Shipped"`.)
- ⛔ **وممنوع من `currentSubtotalLineItemsQuantity > 0`.** المرتجع **الجزئي**
  (بند راجع وبند متساب) بيدّي نفس الرقم زي الاستبدال بالظبط.
- ⛔ **وممنوع من `fulfillableQuantity > 0`** (قاعدة `exchangeItems` في أداة
  الطباعة). قطعة الاستبدال بتبقى **متعمللها fulfillment خلاص** وقت المانفيست
  — `#53987` قطعة الاستبدال عليها `fulfillableQuantity = 0`، فالقاعدة دي كانت
  هتقول «استرجاع» على أوردر استبدال.
- 🔴 **R7 بالحرف** — فلترة `CANCELED`/`DECLINED` وترتيب بـ `createdAt` مش
  بترتيب المصفوفة. أوردر بدورة قديمة مقفولة + دورة جديدة كان هياخد نوع عشوائي.

### `DETAILS_CHUNK = 10`

بلوك `returns` بيرفع تكلفة النداء الواحد، و`MAX_ORDER_IDS = 50` في نداء واحد
ممكن يعدّي سقف تكلفة الاستعلام عند شوبيفاي — وساعتها **الدفعة كلها بترجع
فاشلة والمانفيست مايتطبعش خالص**. نفس درس `STATES_CHUNK_RETURNS` في v4.5.0.

### `missing[]` — الأوردر اللي شوبيفاي ما رجّعتوش

`nodes(ids:)` بترجّع `null` للـ ID اللي ما اتحلّش، والحلقة بتتخطّاه. الواجهة
كانت بترسم صف بـ`—` في كل خانة و**COD صفر**، والصفر ده كان **بيتحسب في
المجموع**. دلوقتي: `—` في خانة COD · **مش داخل في أي مجموع** · تحذير أحمر فوق
الجدول في الورقة نفسها · وتوست على الشاشة (الموظف ممكن يبعت الورقة للطابعة من
غير ما يقراها).

### المجموع بقى تلاتة مش واحد

«إجمالي التحصيل» · «إجمالي الاسترداد» · «الصافي المستحق» — فوق وتحت الجدول.
رقم صافي واحد على ورقة فيها الاتنين بيخفي حركة الفلوس الحقيقية.

🔴 **`MIN_WORKER_VERSION` اترفعت لـ `4.7.0` — والترفيع ده منع ضرر مش تحذير.**
Worker 4.6.0 بيرجّع **نفس اسم الحقل** (`cod`) بقيمة محسوبة بالصيغة القديمة،
يعني التدهور **مش آمن ولا معلن**: الورقة بتخرج من الطابعة بأرقام فلوس غلط
وصفر خطأ في الكونسول. ⛔ متنزّلوش.

✅ **صفر تغيير في `§CONTRACT::extra` أو `TOOL_NAME`** — `order_details` قراءة
بحتة ومابتكتبش أي صف D1، و`cod-payment-center-worker` مابيقراش المسار ده خالص.
`§TODAY-IMPORT` **ما اتلمسش** ومش محتاج يتعدّل مع التسليم ده.

## `package_whereabouts_s1`/`_s2` — كتابة إضافية best-effort (v4.6.0)

مع كل `update_status` ناجح، الأداة بتكتب معاه (نداء `metafieldsSet`/
`metafieldsDelete` **منفصل** بعد كتابة S1/S2 — فشله `warning` مش `error`):

| الحالة المكتوبة | القيمة |
|---|---|
| `Ready` · `Returned` · `Cancelled` | `Office` |
| `Shipped` | `Courier` |
| `Delivered` | مسح القيمة (`metafieldsDelete`) |

على `_s1` أو `_s2` حسب الـ `field` بتاع الـ specifier المتحلّل (نفس منطق
`resolveSpecifier`) — مش حسب حالة الأوردر الحالية.

🔴 **ده تعارض صريح ومقصود مع `ecommoda-order-lifecycle` Rule 17 +
`references/package-whereabouts.md`:**

1. **المصدر الرسمي في المهارة ٣ أدوات مخصّصة** (تغليف · شحن · نقل مخزن↔مكتب)
   بتكتب بناءً على **حركة فيزيائية فعلية** للطرد — مش استنتاج من S1/S2
   (Rule 3 في المهارة: "Never infer whereabouts from S1/S2"). الأداة دي
   بتعمل بالظبط اللي المهارة بتحذّر منه — استنتاج من الحالة.
2. **النطاق في المهارة محصور** على `zone ∈ {Cairo+Giza, Show_Room}` (مفيش
   مكتب في رحلة أوردرات `Other_Regions`/بوسطة). الأداة دي **بتكتب على كل
   الأوردرات بغض النظر عن `zone`** — قرار صريح من أحمد (15-09-2026)، مش سهو.
3. **`Cancelled` بتكتب `Office` دايمًا**، حتى لو الأوردر اتلغى من مرحلة
   مبكرة (`New Order → Cancelled`) قبل ما يتغلف أصلاً ولسه ما خرجش من
   المخزن — تبسيط متعمّد (قرار أحمد 15-09-2026)، مش قراءة دقيقة لموقع الطرد.

**القرار (أحمد، 15-09-2026):** الكتابة دي **إضافية** جنب الـ٣ أدوات، مش بديل
عنهم ومش مصدر حقيقة. اعتبر القيمة هنا **تقريب سريع** من حالة الأوردر، مش
دليل عهدة فعلي — نفس تحذير المهارة نفسها في §6 القاعدة ٥ ("A missing/blank
value... is a data gap, not evidence the package is safe") بينطبق بالعكس
كمان: قيمة موجودة هنا مش دليل كافي، خصوصًا على أوردرات ممكن تكون اتشحنت
ببوسطة فعليًا رغم `zone = Cairo+Giza` (استثناء §3.2 في المهارة، v1.8.0).

المهارة محتاجة تحديث يسجّل الأداة دي كـ **رابع مصدر (best-effort)** في جدول
"من بيكتب إيه" — مسجّل في `skills-updates-2026-09-15.md`.

## المضبوط فعليًا في الداشبورد

> اللي **متظبط بالفعل** — مش اللي المفروض يكون.

```
Bindings : DB → ecommoda-dev-logs
Secrets  : WORKER_SECRET · CLIENT_ID · CLIENT_SECRET
Vars     : SHOP_DOMAIN · LOCATION_ID        ← من [vars] في wrangler.toml
Build watch paths : index.js · wrangler.toml (مضيّق فعلاً — مش * الافتراضي)
```

**تصنيف الـ `env.*` (إجراء §4-أ-٢ في `ecommoda-tool-migration-playbook`):**

| النوع | المتغيرات | إزاي تتأكد |
|---|---|---|
| **Secret** | `WORKER_SECRET` · `CLIENT_ID` · `CLIENT_SECRET` | مستحيلة القراءة — من مصدر أحمد |
| **Var بيرمي لو غاب** | `SHOP_DOMAIN` · `LOCATION_ID` | محروسين بـ `assertEnv` / `requireLocationId` — بيرموا خطأ **باسم المتغير**، و`?action=diag` بيكشفهم |
| **Var ليه fallback** | **لا شيء** ✅ | مفيش أي `env.X \|\| default` في الكود — يعني الأداة دي **مالهاش** سيناريو "أرقام غلط بصمت" من متغير ضايع |

## CORS

`ALLOWED_ORIGINS` صارمة (`https://ecommoda-dev.github.io` بس) — لأن الأداة
**أداة كتابة** بتعدّل حالة أوردرات حقيقية. مفيش wildcard.
✅ الدومين المهجور `ecommoda24.github.io` **مش موجود** في الكود ده (اتنضّف في v3.3.0).

## خط الأساس بعد النقل

خط الأساس هنا **مشتق من D1** (الأداة مالهاش زرار "تحديث" بيرجّع عدّاد ثابت —
مخرجها عملية كتابة). الاستعلام ده هو المرجع، وبيتقارن قبل/بعد:

```sql
SELECT type, COUNT(*) AS n, MAX(timestamp) AS last_row FROM logs WHERE tool = 'order_status' GROUP BY type;
```

القراءة قبل النقل — **31-08-2026**:

```
update : 11,455 صف   (أول صف 15-03-2026 · آخر صف 31-08-2026 13:50Z)
login  :    124 صف   (آخر صف 31-08-2026 13:48Z)
logout :      2 صف
```

✅ **الإثبات بعد النقل = صف `update` جديد بتوقيت بعد الربط.** أي صف جديد معناه
إن الشوبيفاي والـ D1 والأسرار كلهم شغالين على النسخة المنشورة من git.

## 🔴 حارس `ExcelJS` في تصدير السجل (17-09-2026)

`new ExcelJS.Workbook()` في `buildAndDownloadWorkbook` كان **عاري** — والمكتبة
من CDN. CDN محجوب أو شبكة واقعة = `ReferenceError` **عند الضغطة**، والدالة
`async` ومتندّهة من `onclick` فالرمي بيتحوّل **رفض غير ممسوك**.

| الزرار | اللي كان بيحصل | دلوقتي |
|---|---|---|
| **تصدير المحدد** | 🔴 **ولا رسالة خالص** | «مكتبة التصدير ما اتحمّلتش — راجع الاتصال وحدّث الصفحة» |
| **تصدير الكل** | «تعذّر التصدير: ExcelJS is not defined» — إنجليزي تقني | نفس الرسالة العربية |

- 🔴 **والحارس لوحده ماكانش كفاية** — `buildAndDownloadWorkbook` بترجّع
  `false` عند الفشل، و**المستدعيين كانوا بيتجاهلوا الرجوع**: «تصدير المحدد»
  كان بيقول «✅ تم تصدير N صف» بعدها على طول. يعني حارس بلا قراءة الرجوع كان
  هيحوّل الصمت لـ**ادعاء نجاح كاذب** — أسوأ. فالتعديل **تلات أماكن**:
  الحارس + `if (await …)` في «تصدير الكل» + `if (!await …) return;` في
  «تصدير المحدد».
- ⚠️ **وترتيب الحارس بعد فحص «مفيش بيانات» مقصود** — «لا توجد بيانات
  للتصدير» أدقّ من «المكتبة ما اتحمّلتش» على سجل فاضي.
- ⛔ **ولازم المولّد يتشغّل بعد التعديل ده** — نسخة المركز متولّدة من الملف
  ده، وأربع بنود في `docs/tools-check.mjs` §⑦ بتحجب الـ CDN فعلاً وبتقيس
  الرسالة.

## فخاخ الأداة دي

- 🔴 **قايمة الأسباب بتتغيّر من داشبورد شوبيفاي، والأداة بتكاشها.** التعديل
  حصل مرتين في ٤ أيام (03→07-09-2026). من v4.5.0 فيه TTL ١٠ دقايق + تحديث
  إجباري مع كل اختيار حالة، **و** تحقق سيرفري قبل أي فعل لا رجعة فيه. قبل
  كده: `#52732` اتلغى على شوبيفاي وحالته ما اتكتبتش واتصلّح باليد.
  ⚠️ **٢٩ أوردر تاريخي شايلين `cancel_manual_reason = "لا يوجد سبب"`** —
  قيمة **خارج** قايمة الاختيارات الحالية. أي `metafieldsSet` مستقبلي بيلمس
  الحقل ده عليهم هيفشل. (أحمد بيصلّحهم بنفسه — 08-09-2026.)
- 🔴 **`In-Return` كانت ناقصة من جداول الانتقال بالكامل لحد v4.5.0** — والفخ
  إن العطل **مخفي بالتصميم**: `In-Return` بتتكتب من مزامنة بوسطة بس، يعني
  على أوردرات `Other_Regions` بس. اللي بيشتغل على القاهرة/الجيزة عمره ما
  هيشوفه. أي قاعدة انتقال جديدة تتفحص على **الزونين**.
- ⚠️ **الجداول متكرّرة في ملفين** (`TRANSITION_RULES` في `index.js` و
  `index.html`). فحص المطابقة مش تلقائي — نفّذه بعد أي تعديل:
  ```bash
  node -e "…"   # المقارنة الكاملة في order-status-updater-review.md
  ```
- ✅ **(اتقفل في v4.5.0)** الثابت `CAIRO_OFFSET_HOURS = 3` كان في الملفين
  وكان هيغلط بساعة من **29-10-2026** — في الوقت المعروض، وفي فلاتر التاريخ،
  و**في `custom.pickup_date` اللي بيتكتب على الأوردر**. بقى محسوب بـ
  `Intl` / `Africa/Cairo`. ⚠️ الفخ ده **موجود في أدوات تانية في الستاك**.
- ✅ **(اتقفل في v4.4.0)** بلوك الرأس في `index.js` كان مكتوب `v4.3.1` والثابت
  `VERSION` مكتوب `4.3.0`. الاتنين بقوا `4.4.0`.
- ✅ **(اتقفل في v4.4.0)** الواجهة كانت بتقرا رابط الـ Worker من `localStorage`،
  فالموظف اللي بيفتح الرابط أول مرة كان لازم يدخل الإعدادات. دلوقتي
  `WORKER_URL` و`ADMIN_WORKER_URL` **constants في `§CONFIG`** (Standards #28)،
  و`order_status_worker_secret` هو مفتاح الـ `localStorage` الوحيد الباقي.
  ⚠️ المفتاحين القدام (`order_status_worker_url` · `admin_worker_url`) **ما
  اتمسحوش** من متصفحات الموظفين — بقوا مهملين بس، والأداة مابتقراهمش.

## استرجاع النسخ القديمة

> ده بديل الـ tags — دفع الـ tags ممنوع من جلسات Claude Code السحابية.

```
النسخ المرقّمة القديمة (3.4.0 · 3.4.1 · 3.4.2 · 3.5.0) محفوظة في commit: b1e37b4
git show b1e37b4:3.5.0.html
```

`Index.html` القديم و`index.html` الجديد ليهم **نفس الـ blob SHA**
(`65e4b2a850a083ed94bd73a9c1d92518de496381`) — إثبات إن مفيش بايت اتغيّر في
الواجهة أثناء النقل.

## بصمة المهارات

| المهارة | الإصدار وقت آخر تعديل |
|---|---|
| ecommoda-worker-builder | v3.7.0 |
| ecommoda-html-builder | v6.6.0 |
| ecommoda-constants | v3.1.0 |
| ecommoda-order-lifecycle | v1.8.0 |

آخر مطابقة: 24-09-2026 · `index.js` v4.7.1 · `index.html` v4.6.0

## 🔴 الطبقة ٥ — الحارس الديناميكي لقيم اللوج (24-09-2026 · v4.7.1)

`check-log-values.mjs` اتستبدل بالنسخة المصلَّحة (`ecommoda-worker-builder`
Step 7): النسخة القديمة كانت بتدوّر على `type:` بنقطتين بس، فـ object
shorthand (`{ tool, type }`) كان بيعدّي في صمت وبيدّي `exit 0` وهو شايف جزء
من القيم بس. الأداة دي **مالهاش** الفخ ده أصلًا — التلات نداءات
(`login`/`logout`/`update`) كلهم `type:` نص ثابت صريح، ومفيش `writeLogsBatch`
ولا أنكور تاني في الملف.

**اتنفّذ:**
- `LOG_REGISTRY` (§LOG-REG فوق `writeLog` في `index.js`) — `order_status`:
  `login` · `logout` · `update`، مبني من `log-values.json`.
- `writeLog` بقى بيكتب الصف **دايمًا**، ويعلّم `extra._unregistered = true`
  + UPSERT صامت في `log_value_alerts` (جدول مشترك على مستوى الستاك، اتعمل
  قبل كده — ما اتعملش هنا `CREATE TABLE`) لو الزوج `(tool, type)` مش مسجّل.
  **مفيش رفض كتابة أبدًا.**
- `dynamicTypes` فضلت فاضية في `log-values.json` — مفيش قيمة `type` بتتحسب
  وقت التشغيل في الملف ده.
- `node check-log-values.mjs` بعد التحديث: **exit 0** (٣ قيمة مسجّلة · ٣
  مستخدمة · صفر تحذيرات).
🔴 **معلّق ومطلوب قبل أي طباعة مانفيست جديدة — Promote للـ Worker v4.7.0.**
من غيره الواجهة v4.6.0 هتولّع «الـ Worker نسخة قديمة» (`MIN_WORKER_VERSION = 4.7.0`)
و**الأخطر**: لو حد طبع من واجهة قديمة متكاشة في متصفح، الورقة هتخرج بأرقام
الفلوس الغلط تاني وصفر خطأ في الكونسول. ⚠️ والتحقق بعد الـ Promote **مش** إن
الورقة طلعت: افتح مانفيست فيه أوردر استرجاع واحد على الأقل وتأكد إن الرقم
**بالسالب** وإن عمود «نوع الأوردر» بيقول «استرجاع».

🔴 **ومعلّق كمان من 16-09-2026 — تدوير `WORKER_SECRET` لمجموعة
`delivery_cod_ops`** (قرار الدمج تحت مركز الشحن والتحصيل · القسم 🔗 فوق).
من غيره `order-status.html` في المركز بترجّع `401` على كل نداء. ⚠️ والرابط
القديم بتاع الأداة دي هيحتاج القيمة الجديدة كمان.

🔴 معلّقة: **٤ تعديلات مطلوبة في المهارات** — التفاصيل والصياغة الجاهزة في
`skills-updates-2026-09-08.md` و`skills-updates-2026-09-15.md` (تتنفّذ في
جلسة منفصلة):
1. `order-lifecycle` §2.2 — `S2: In-Return → Ready` (قرار جديد، مطبَّق هنا)
2. `order-lifecycle` Rule 5 · §1.3 · §4 — إلغاء الـ Fulfillment في لوب إعادة التسليم
3. `order-lifecycle` §1.5 — «لا يوجد سبب» اتشالت واتستبدلت بـ «العميل رفض ذكر السبب»
4. `order-lifecycle` `references/package-whereabouts.md` §5 — تسجيل الأداة دي
   كرابع مصدر (best-effort، كل الأوردرات بغض النظر عن `zone`) — قرار أحمد
   15-09-2026، مطبَّق هنا (v4.6.0)

> بصمة الـ Worker منقولة **زي ما هي** من بلوك الرأس في الكود المنشور — النقل
> ماغيّرش سطر واحد في `index.js`، فالبصمة تفضل على تاريخها الحقيقي.
> `index.html` اتختم في النقل نفسه (Step 6 بيعتبر «النقل لـ git» سبب حقيقي).
> و`v1.0.0` معناها **«ما قبل النظام»** (Step 5) — **مش** شهادة إن الواجهة
> اتراجعت على html-builder v1.0.0. الواجهة ما اتفتحتش للمراجعة في النقل ده.

## مسائل مفتوحة

- **`S1: In-Return → Ready` — سؤال مفتوح.** النظير في S2 اتفتح في v4.5.0
  بقرار صريح؛ نظيره في S1 (أوردر راجع من بوسطة يرجع لدورة تسليم جديدة بدل
  ما يتقفل Returned) **مش** في الجدول المعتمد ولا اتقرر. لو الرد أيوه،
  التعديل سطر واحد في `Ready.s1From` + `TRANSITION_SOURCES['S1:Ready']`
  في الملفين.
- **قايمة `custom.return_manual_reason` فيها ٣ قيم بس** والتوزيع الفعلي
  ٥٢/٥٠/٤٠ — بصمة قايمة أصغر من الواقع، ومفيهاش عيلة أسباب الـ RTO
  (لا يرد · العنوان غلط · تأجيل) اللي موجودة في قايمة الإلغاء. أحمد بيفكّر
  فيها (08-09-2026) — **ممنوع** بناء أي KPI على أسباب الإرجاع لحد ما تتحسم.
- **زرار الخروج هو اسم الموظف نفسه** (١٣٨ دخول مقابل ٢ خروج) ولسه بـ
  `confirm()` أصلي. أحمد هيعيد تصميم الجزء ده بالكامل في المهارة لاحقًا —
  متلمسوش قبل كده (قرار 08-09-2026).
- **صفر اختصارات كيبورد في مسار التشغيل الأساسي** — **مقصود** (قرار أحمد
  08-09-2026). الاستثناء الوحيد المضاف في v4.5.0: `Enter`/`Esc` على نافذة
  ملخص التحديث، عشان أول سكانة بعد الدفعة ماتضيعش.
- ✅ **(اتعمل في v4.5.0)** شكل `checks` في `?action=diag` بقى مصفوفة
  `[{ ok, label, detail }]` — **مع الإبقاء على المفاتيح القديمة** فترة
  انتقالية عشان أي واجهة متكاشة في متصفح موظف ما تكسرش.
- ✅ **(اتأكد 15-09-2026 من سكرين شوت داشبورد كلاودفلير، تاريخ التنفيذ الفعلي
  مش معروف)** `Build watch paths` **مضيّق فعلاً** لـ `index.js` +
  `wrangler.toml` — كان هنا موثّق غلط إنه لسه `*` الافتراضي. أي ملف جديد
  الـ Worker يعتمد عليه لازم **يتضاف للقايمة يدويًا** — التضييق مش بيتوسّع
  لوحده، وملف منسي هنا = تعديل عليه مايعملش build من غير أي تحذير.
- ✅ **(اتعمل في v4.4.0)** معيار #28 — `WORKER URL` و`ADMIN WORKER URL` بقوا
  constants في `§CONFIG`. قيمة `ADMIN_WORKER_URL` من `ecommoda-constants` §5b
  (`employees-admin-panel-worker`).
آخر تحديث: 16-09-2026 — v4.7.0/v4.6.0 (عمود COD في المانفيست بقى `totalOutstandingSet` موقّع · عمود «نوع الأوردر» · `missing[]` · مجموع تلاتي)

🔗 16-09-2026 — **الأداة اندمجت تحت `Delivery-COD-Operations-Center`**
(`order-status.html`): دخول واحد وسر واحد للمركز. **صفر تعديل في `index.js`
وصفر تعديل في `index.html` هنا** — نسخة المركز **متولّدة** من الملف ده. التفاصيل
والمطلوب في القسم 🔗 أول الملف.

</div>
