<div dir="rtl" style="text-align: right;">

# تعديلات مطلوبة في المهارات — 15-09-2026

![version](https://img.shields.io/badge/version-v1.0.0-blue)

الملف ده **مخرج** تسليم `Order-Status-Updater` v4.6.0 (كتابة
`custom.package_whereabouts_s1`/`_s2` مع كل تحديث حالة) + قرارات أحمد اللي
اتاخدت وقت الجلسة (راجع `AskUserQuestion` في نفس التسليم). البند فيه
الصياغة الجاهزة عشان يتنفّذ في جلسة منفصلة من غير إعادة تحقيق.

> ⚠️ **الأداة اتعدّلت بالفعل** (v4.6.0 منشورة). التعديل ده على **المهارة**
> عشان القرار يبقى مكتوب في المصدر الحاكم، مش في أداة واحدة.

## فهرس سريع

| # | المهارة | النوع | البند |
|---|---|---|---|
| ١ | `ecommoda-order-lifecycle` | 🟢 مُستحسن | `package-whereabouts.md` §5 — `Order-Status-Updater` كرابع مصدر (best-effort) |

---

# `ecommoda-order-lifecycle`

**الإصدار الحالي:** v1.8.0 → المقترح **v1.9.0** (بند واحد، سياق جديد على
قاعدة موجودة — مفيش قاعدة قديمة اتقلبت، فـ MINOR).

---

## ١ — `Order-Status-Updater` كرابع مصدر لـ `package_whereabouts_s1`/`_s2` — best-effort 🟢

**القرار (أحمد، 15-09-2026):** بالإضافة للـ٣ أدوات المخصّصة في §5 (التغليف ·
الشحن · نقل مخزن↔مكتب)، `Order-Status-Updater` بقى بيكتب الحقلين دول
**كأثر جانبي** لكل تحديث حالة ناجح (`update_status`) — مش كخطوة فيزيائية
موثّقة زي الـ٣ أدوات.

**الفرق الجوهري عن الـ٣ أدوات، ولازم يتسجّل صريح:**

- الـ٣ أدوات بتكتب بناءً على **حركة فيزيائية فعلية شافها الموظف وقت الكتابة**
  (تغليف حصل · تسليم للمندوب حصل · نقل بين المخزن والمكتب حصل) — بيطبّقوا
  Rule 3 بالحرف.
- `Order-Status-Updater` بيكتب **استنتاج من قيمة S1/S2 المكتوبة**، من غير أي
  معرفة بموقع الطرد الفعلي. ده **نقيض Rule 3** ("Never infer whereabouts
  from S1/S2") — مقبول هنا لأنه **best-effort إضافي** موصوف صراحة، مش لأن
  القاعدة اتغيّرت.
- الكتابة **مش محصورة على `zone ∈ {Cairo+Giza, Show_Room}`** زي §2 — بتحصل
  على **كل** الأوردرات، بما فيهم `Other_Regions` (بوسطة) اللي المهارة بتقول
  إن الحقل أصلاً برّه نطاقهم. قرار صريح من أحمد، مش سهو.

**المابينج المطبَّق (`index.js` §CONSTANTS::packageWhereabouts، v4.6.0):**

| الحالة المكتوبة (S1 أو S2) | `package_whereabouts_{s1,s2}` |
|---|---|
| `Ready` · `Returned` · `Cancelled` | `Office` |
| `Shipped` | `Courier` |
| `Delivered` | مسح القيمة (`metafieldsDelete`) |

`_s1` أو `_s2` حسب `field` الـ specifier المتحلّل — نفس منطق اختيار الحقل في
باقي الأداة، مش قراءة زون أو تخمين.

⚠️ **`Cancelled → Office` مش دقيقة دايمًا:** أوردر اتلغى من `New Order` قبل
ما يتغلف أصلاً بيتكتب عليه `Office` برضه — تبسيط متعمّد (قرار أحمد)، القيمة
مش قراءة فعلية لموقع الطرد في الحالة دي.

**المكان المقترح للتعديل:** `references/package-whereabouts.md` §5 (جدول
"من بيكتب إيه") — إضافة صف رابع:

```markdown
| # | Tool | Status | Writes |
|---|---|---|---|
| 1 | Packing tool … | planned update | … |
| 2 | Existing shipping tools … | planned update | … |
| 3 | New warehouse tool … | planned build | … |
| 4 | `Order-Status-Updater` (v4.6.0) | 🚧 **live، best-effort — ليس المصدر الرسمي** | كل تحديث S1/S2 ناجح بيكتب قيمة مشتقة من الحالة (Ready/Returned/Cancelled→Office · Shipped→Courier · Delivered→مسح) على `_s1`/`_s2` حسب field المتحلّل، **على كل الأوردرات بغض النظر عن zone** — بخلاف §2 وبخلاف Rule 3 (استنتاج من S1/S2 عمدًا، مش حركة فيزيائية مُشاهَدة) |
```

**وأضِف تحت الجدول تحذير:**

> ⚠️ **المصدر ٤ (`Order-Status-Updater`) لا يتبع Rule 3 ولا §2 عمدًا — القيمة
> منه تقريب، مش دليل عهدة.** لو الحقل اتكتب من المصدرين ٤ والـ٣ الأولانيين
> على نفس الأوردر بقيم مختلفة، **الأولوية للمصدر اللي شاف الحركة الفعلية**
> (١-٣)، مش لآخر كتابة زمنيًا. أي أداة قراءة لازم تفترض التعارض ده ممكن يحصل
> لحد ما الـ٣ أدوات الرسمية تكتمل (راجع G-17).

**ولازم يتسجّل في `known-gaps.md` G-17:** المصدر الرابع ده بيقلّل من قيمة
الحقل كـ"دليل عهدة" — وجود قيمة دلوقتي ممكن يكون من `Order-Status-Updater`
(تقريب) مش من أحد الـ٣ أدوات الرسمية (مشاهدة فعلية)، وبدون علم أنهي مصدر
كتب آخر قيمة، الحقل بيرجع لنفس مشكلة "مفيش حد عارف يتأكد" اللي كان قايم
قبل الحقل يتضاف أصلاً.

</div>
