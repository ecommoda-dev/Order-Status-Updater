<div dir="rtl" style="text-align: right;">

# تعديلات مطلوبة في المهارات — 08-09-2026

![version](https://img.shields.io/badge/version-v1.0.0-blue)

الملف ده **مخرج** مراجعة `Order-Status-Updater` العميقة
(`order-status-updater-review.md`) + قرارات أحمد اللي اتاخدت في نفس الجلسة.
كل بند فيه **الصياغة الجاهزة** عشان يتنفّذ في جلسة منفصلة من غير إعادة تحقيق.

> ⚠️ **الأداة اتصلّحت بالفعل** (v4.5.0 منشورة). التعديلات دي على **المهارات**
> عشان القرار يبقى مكتوب في المصدر الحاكم، مش في أداة واحدة — وإلا أول أداة
> جديدة هتقع في نفس الحفرة.

## فهرس سريع

| # | المهارة | النوع | البند |
|---|---|---|---|
| ١ | `ecommoda-order-lifecycle` | 🟢 مُستحسن | `S2: In-Return → Ready` — انتقال جديد معتمد |
| ٢ | `ecommoda-order-lifecycle` | 🔴 كاسر | إلغاء الـ Fulfillment في لوب إعادة التسليم — القاعدة معكوسة |
| ٣ | `ecommoda-order-lifecycle` | 🔴 كاسر | «لا يوجد سبب» اتشالت من قايمة الاختيارات |
| ٤ | `ecommoda-order-lifecycle` | ⚪ تحريري | §1.5 لسه بيقول إن الأداة «مالهاش ريبو» |
| ٥ | `ecommoda-order-lifecycle` | ⚪ تحريري | §2.3 — الشرط الملزم اتفحص وطلع غير منطبق |
| ٦ | `ecommoda-worker-builder` | 🟢 مُستحسن | **بند جديد**: الأفعال بعد الفعل اللي مالوش رجعة |
| ٧ | `ecommoda-worker-builder` | 🟢 مُستحسن | **بند جديد**: حارس السر الغايب |
| ٨ | `ecommoda-worker-builder` | 🟢 مُستحسن | **بند جديد**: التوقيت يتحسب مايتكتبش |
| ٩ | `ecommoda-worker-builder` | 🟢 مُستحسن | **بند جديد**: سقف صريح على عدد الـ IDs وتكلفة الاستعلام |
| ١٠ | `ecommoda-html-builder` | 🟢 مُستحسن | **بند جديد**: الإدخال يتقفل أثناء أي عملية دفعية |
| ١١ | `ecommoda-html-builder` | 🟢 مُستحسن | **بند جديد**: المدخل المجهول يترفض وقت الإدخال |
| ١٢ | `ecommoda-html-builder` | 🟢 مُستحسن | **بند جديد**: الكاش المشتق من إعدادات لازم TTL |
| ١٣ | `ecommoda-html-builder` | 🟢 مُستحسن | Standards #41 — `.toast-warning` مفقودة فعليًا |
| ١٤ | `ecommoda-html-builder` | 🟢 مُستحسن | Standards #29 — الفحص الآلي مابيمسكش الرقم المتأخر |
| ١٥ | `ecommoda-html-builder` | 🟢 مُستحسن | Step 9A — البارسر مابيمسكش المستند المولّد |

---

# القسم أ — `ecommoda-order-lifecycle`

**الإصدار الحالي:** v1.3.0 → المقترح **v1.4.0** (فيه بندان كاسران).

---

## ١ — `S2: In-Return → Ready` — انتقال جديد معتمد 🟢

**القرار (أحمد، 08-09-2026):** الأوردر اللي في `S2 = In-Return` (القطعة راجعة
مع بوسطة) **ينفع يرجع لـ `S2 = Ready`** — القطعة بترجع للمكتب وممكن تطلع في
دورة تسليم جديدة، أو العميل يلغي الطلب أصلاً.

**المكان:** `references/state-machines.md` §2.2 (مخطط تدفق S2).

**الصياغة المقترحة — استبدل مخطط §2.2 بده:**

```
S1 = Delivered
      │
      ▼
Confirmed + RETURN ─┐
                    ├─► Ready ─► Shipped ─► [In-Return] ─► Returned
Confirmed + EXCHANGE┘             ▲              │              │
                                  └──────────────┘              │
                                   محاولة تسليم جديدة            │
        (after an exchange, a new R/E cycle can start again) ────┘
```

**وأضِف تحته:**

> **`In-Return → Ready` (S2) — مسموح.** (قرار أحمد 08-09-2026.) القطعة
> بترجع للمكتب، وساعتها فيه مسارين: دورة تسليم جديدة (`Ready`) أو الأوردر
> يتقفل (`Returned`). ده **نظير** لوب إعادة التسليم في S1 (§1.3) بالظبط.
>
> ⚠️ **النظير في S1 (`In-Return → Ready`) لسه مش معتمد** — سؤال مفتوح، مش
> مطبَّق في أي أداة، ومايتفترضش إنه موجود.

**وفي §2.2 جدول الانتقالات (لو فيه واحد صريح)، الصف بيبقى:**

| From | Allowed next |
|---|---|
| `In-Return` (S2) | `Returned` · **`Ready`** *(محاولة تسليم جديدة — 08-09-2026)* |

**⚠️ بند مصاحب مهم — يتكتب في §2.2 كمان:**

> 🔴 **`In-Return` بتتكتب من مزامنة بوسطة بس**، يعني على أوردرات
> `zone = Other_Regions` بس. أي أداة بتنسى تدعم `In-Return` كمصدر انتقال
> **بتشتغل تمام** على القاهرة/الجيزة و**بتتوقف تمامًا** على أوردرات بوسطة —
> والعطل مخفي بالتصميم عن اللي بيختبر بأوردرات محلية.
>
> **حصل فعليًا:** `Order-Status-Updater` من v3.0.0 لحد v4.5.0 ماكانتش
> بتقبل `In-Return` كمصدر خالص. النتيجة: كل أوردر بوسطة بيوصل `In-Return`
> كان **مقفول نهائيًا** في الأداة، و`reverseFulfillmentOrderDispose`
> عمره ما بيشتغل عليه — يعني **المخزون مايرجعش**. ٤ أوردرات كانت معلّقة وقت
> الاكتشاف (#52457 · #52704 · #53027 · #53517)، أقدمها ١٠ أيام، **كلها**
> `Other_Regions`.
>
> **قاعدة الاختبار:** أي قاعدة انتقال جديدة تتفحص على **الزونين** — مش على
> أوردر محلي واحد.

---

## ٢ — إلغاء الـ Fulfillment في لوب إعادة التسليم — القاعدة معكوسة 🔴

**ده أخطر بند في الملف** — المهارة بتقول حاجة والواقع بيقول عكسها، من ٦٩ حالة.

**المكان:** SKILL.md **Rule 5** · `state-machines.md` §1.3 · §4.

**النص الحالي (Rule 5):**

> **`Ready` + Shopify `Fulfilled` is legitimate, not a bug.**
> Fulfillment is **never cancelled** when an order comes back for a
> re-delivery attempt. Any "ready to ship" count must separate:
> - `Ready` + **not** Fulfilled → first dispatch
> - `Ready` + **Fulfilled** → returning for a repeat attempt

**القرار (أحمد، 08-09-2026):** إلغاء الـ fulfillment **صح ومقصود** — الأوردر
بيرجع للمكتب لحد ما تتعمل دورة تسليم جديدة، وممكن العميل يلغيه أصلاً، فلازم
يرجع `UNFULFILLED` عشان `Ready → Cancelled` تفضل صالحة (§1.2 بتقول الإلغاء
لازم يحصل **قبل** ما الأوردر يبقى Fulfilled).

**الدليل المقاس (08-09-2026):**

| القياس | النتيجة |
|---|---|
| انتقالات `Shipped → Ready` ناجحة في D1 (`order_status`) | **٦٩** — آخرها 07-09 |
| أوردرات عليها `manual_status = Ready` في المتجر | ٤٠+ |
| منهم `displayFulfillmentStatus = FULFILLED` | **صفر** |

يعني الحالة اللي Rule 5 بتوصفها **مش موجودة في الداتا خالص**.

**الصياغة المقترحة — استبدل Rule 5 بالكامل:**

> 5. **`Ready` بعد `Shipped` = الأوردر رجع المكتب، والـ fulfillment بيتلغى.**
>    (اتصحّحت 08-09-2026 — القاعدة القديمة كانت بتقول العكس.)
>
>    لما الأوردر يرجع من عند المندوب من غير تسليم، الأداة بترجّعه `Ready`
>    **وبتلغي الـ fulfillment على شوبيفاي**. ده مقصود: الأوردر بيستنى في
>    المكتب لحد دورة تسليم جديدة، وممكن العميل يلغيه أصلاً — و§1.2 بتقول
>    الإلغاء لازم يحصل قبل ما الأوردر يبقى Fulfilled، فلو الـ fulfillment
>    فضل قايم يبقى `Ready → Cancelled` مخالفة لجدولنا.
>
>    🔴 **النتيجة اللي لازم كل داشبورد يعرفها: التمييز بين «شحنة أولى»
>    و«محاولة مكررة» مش قابل للاسترجاع من حالة الـ fulfillment.** الاتنين
>    بيبقوا `Ready` + `UNFULFILLED`. المصدر الوحيد هو **سجل D1**:
>
>    ```sql
>    -- محاولات إعادة التسليم
>    SELECT date(timestamp) d, COUNT(*) n FROM logs
>    WHERE tool = 'order_status' AND type = 'update'
>      AND json_extract(extra, '$.targetLabel') = 'Ready'
>      AND json_extract(extra, '$.s1Before')    = 'Shipped'
>      AND json_extract(extra, '$.result')      = 'success'
>    GROUP BY d ORDER BY d DESC;
>    ```
>
>    ⚠️ ده تطبيق مباشر لـ **Rule 9** (المدد والعدادات مصدرها سجل D1 مش
>    الميتافيلد) — والحالة دي مثال نموذجي ليها، تستاهل تتذكر في الاتنين.

**وفي `state-machines.md` §1.3 — استبدل التحذير:**

```
> ⚠️ **الـ Fulfillment بيتلغى على شوبيفاي في اللوب ده** (اتصحّح 08-09-2026).
> الأوردر بيرجع `UNFULFILLED` عشان يفضل قابل للإلغاء (§1.2). يعني `Ready`
> **دايمًا** `UNFULFILLED` — وعدد «المحاولات المكررة» بيتقرا من سجل D1 بس.
```

**وفي §4 (العلاقة بحالات شوبيفاي) — الصف بيبقى:**

| EcomModa state | Native Shopify |
|---|---|
| `Ready` after a failed attempt | **UNFULFILLED** — الـ fulfillment بيتلغى (08-09-2026) |

**⚠️ نطاق أوسع — لازم يتفحص قبل الإغلاق:** أي داشبورد أو تقرير في الستاك
بيقرا `Ready` + `FULFILLED` كإشارة «محاولة مكررة» بيرجّع **صفر** دلوقتي.
دوّر بـ:

```bash
grep -rn "FULFILLED" --include="*.js" --include="*.html" . | grep -i "ready\|attempt\|محاول"
```

---

## ٣ — «لا يوجد سبب» اتشالت من قايمة الاختيارات 🔴

**المكان:** `references/state-machines.md` §1.5 — الصف الثالث في الجدول.

**النص الحالي:**

> | **«لا يوجد سبب» is a real value** | 🔴 Counter-intuitive, and the single
> easiest thing here to "fix" by mistake. An employee explicitly choosing
> *no reason* is a **recorded answer** — not a missing field, not a null,
> not a validation failure. Never normalise it away, never treat it as absence. |

**القرار (أحمد، 08-09-2026):** القيمة **اتشالت واتستبدلت بـ «العميل رفض ذكر
السبب»**. القاعدة زي ما هي مكتوبة بقت غلط.

**الدليل:** تعريف `custom.cancel_manual_reason` النهارده فيه ١٦ قيمة، مفيش
فيهم «لا يوجد سبب»، وفيهم «العميل رفض ذكر السبب». والسجل فيه **٢٩ صف** كتبوا
«لا يوجد سبب» تاريخيًا (تاني أكتر قيمة استخدامًا وقتها).

**الصياغة المقترحة — استبدل الصف بده:**

> | **«الرفض» إجابة، مش غياب** | 🔴 عكس البديهة وأسهل حاجة تتصلّح بالغلط.
> لما الموظف يسجّل إن العميل **رفض يقول السبب**، دي **إجابة مسجّلة** — مش
> حقل ناقص ولا `null` ولا فشل تحقق. ممنوع تطبيعها أو التعامل معاها كغياب.
> **الشكل الحالي للقيمة: `العميل رفض ذكر السبب`** (استبدلت `لا يوجد سبب`،
> 08-09-2026). ⚠️ **المسمّى نفسه قابل للتغيير — القاعدة مش عن نص معيّن،
> هي عن إن *الرفض* قيمة أولى في القايمة.** |

**وأضِف بند جديد تحت الجدول مباشرةً — ده أهم من التصحيح نفسه:**

> 🔴 **قايمة اختيارات الميتافيلد مورد حيّ، والأداة اللي بتكاشها بتكسر.**
> (اتضاف 08-09-2026 من عطل حقيقي.)
>
> قوايم الاختيار (`cancel_manual_reason` · `return_manual_reason` ·
> `courier`) بتتعدّل من داشبورد شوبيفاي **بدون أي إشعار لأي أداة**. قيست
> فعليًا: قايمة الإلغاء اتغيّرت **مرتين في ٤ أيام**.
>
> **العطل الناتج (`#52732`، 03-09-2026، موثّق بالكامل):**
> ```
> ① الواجهة كاشت القايمة أول تحميل للتاب — بلا انتهاء
> ② القيمة اتشالت من تعريف الميتافيلد بعدها
> ③ الموظف اختار القيمة القديمة (لسه معروضة عنده)
> ④ الـ Worker نفّذ orderCancel — **فعل لا رجعة فيه**
> ⑤ metafieldsSet اترفضت: "Value does not exist in provided choices"
> ⑥ النتيجة: أوردر **ملغي** على شوبيفاي و S1 لسه "Ready"
> ⑦ إعادة المحاولة **مستحيلة** (cancelOrder بترفض على أوردر ملغي)
> ⑧ اتصلّح باليد بعد ٢١ ثانية — والسبب الإلزامي اتفقد نهائي
> ```
>
> **قاعدتان إلزاميتان لأي Worker بيكتب قيمة من قايمة اختيار:**
>
> 1. **التحقق قبل الفعل اللي مالوش رجعة.** اجلب القايمة الحية وقارن **قبل**
>    أي `orderCancel` / `refund` / `fulfillmentCancel` — مش بعده.
>    ⚠️ *fail-open*: لو جلب القايمة نفسه فشل، كمّل — شوبيفاي هترفض القيمة
>    الغلط زي الأول. المطلوب منع الحالة **المعروف** إنها هتفشل، مش تحويل
>    فشل شبكة لتوقف كامل.
> 2. **الواجهة ماتكاشش القايمة بلا انتهاء.** TTL قصير (١٠ دقايق) + تحديث
>    إجباري عند نقطة طبيعية في المسار (اختيار الحالة المستهدفة مثلاً).
>
> **دَين بيانات مصاحب:** الأوردرات اللي شايلة قيمة **خارج** القايمة الحالية
> (٢٩ أوردر هنا) = صنف **Rule 13** بالظبط (قيمة مش في القايمة → توسم،
> ماتتحركش). وأي `metafieldsSet` مستقبلي بيلمس الحقل ده عليها **هيفشل**.

---

## ٤ — §1.5 لسه بيقول إن الأداة «مالهاش ريبو» ⚪

**النص الحالي في آخر §1.5:**

> **Where the implementation lives.** Today only **Order Status Updater**
> writes S1 manually, and the field names it uses for the last two rows
> (`reasonIsNew`, `extra.reasonSource`) are that Worker's own. They are named
> here because that tool **has no repo yet** — it is one of the tools still
> deployed by hand. At migration they move to its `CLAUDE.md`, and this
> section keeps the business rules only.

**الواقع:** النقل خلص، الريبو موجود (`ecommoda-dev/Order-Status-Updater`)،
والأسماء متوثّقة في `CLAUDE.md` بتاعه.

**الصياغة المقترحة:**

> **Where the implementation lives.** `Order-Status-Updater` هي الأداة
> الوحيدة اللي بتكتب S1 يدويًا. أسماء الحقول التنفيذية (`reasonIsNew` ·
> `extra.reasonSource`) مكانها **`CLAUDE.md` بتاع ريبو الأداة**
> (`ecommoda-dev/Order-Status-Updater`) — النقل تم، والقسم ده بيمسك القواعد
> التجارية بس.

---

## ٥ — §2.3: الشرط الملزم اتفحص وطلع غير منطبق ⚪

**النص الحالي:**

> ⚠️ **Specified, not verified (26-08-2026).** This gate is Ahmed's
> **decision**, not a confirmed implementation. Nobody has checked that Order
> Status Updater actually rejects these writes. Treat it as a **binding
> requirement for the next piece of work on that Worker**.

**نتيجة الفحص (08-09-2026):** الأداة **مابتكتبش** `Confirmed + RETURN` ولا
`Confirmed + EXCHANGE` خالص — بتكتب في S2 القيم `Ready` · `Shipped` ·
`Returned` بس (`SPECIFIERS` فيها `Ready_S2` · `Shipped_S2` · `Returned_S2`
وبس). فالشرط **غير منطبق** عليها، مش «متجاهَل».

**الصياغة المقترحة:**

> ✅ **اتفحص 08-09-2026 — غير منطبق على `Order-Status-Updater`.** الأداة
> بتكتب في S2 القيم `Ready` · `Shipped` · `Returned` بس، وعمرها ما كتبت
> `Confirmed + RETURN` / `Confirmed + EXCHANGE` — القيمتين دول بيتكتبوا من
> خدمة العملاء في داشبورد شوبيفاي، ومفيش Worker في المسار ده.
>
> **الشرط لسه ساري** على **أي** Worker مستقبلي يكتب القيمتين دول. أول أداة
> تعمل كده تنفّذه (رفض + تسجيل).

---

# القسم ب — `ecommoda-worker-builder`

**الإصدار الحالي:** v2.1.0 → المقترح **v2.2.0** (٤ بنود مضافة، مفيش كاسر).

---

## ٦ — بند جديد في Step 5A: الأفعال بعد الفعل اللي مالوش رجعة 🟢

**المكان:** `Step 5A` — بند جديد **⑩** بعد ⑨، ومدخل في Code Review Checklist.

**الصياغة المقترحة:**

> ### ⑩ ترتيب الأفعال داخل العملية الواحدة — والفشل بعد النقطة اللي مافيش رجوع منها
>
> عملية واحدة على أوردر غالبًا فيها أكتر من فعل: إلغاء · كتابة حالة · تنفيذ ·
> استرجاع مخزون. **الترتيب بيحدد شكل الفشل**، وفيه قاعدتان:
>
> **① كل تحقق ممكن يتعمل — يتعمل قبل أول فعل لا رجعة فيه.**
> `orderCancel` · `refundCreate` · `fulfillmentCancel` مالهمش تراجع. أي فحص
> ممكن يفشل (قيمة من قايمة اختيار · صلاحية · متغير) لازم يتنفّذ **قبلهم**.
>
> ```js
> // ❌ الترتيب اللي أنتج #52732
> await cancelOrder(...);          // لا رجعة فيه
> await setMetafields(...);        // ← اترفضت هنا: أوردر ملغي بحالة قديمة
>
> // ✅
> validateEverythingCheap();       // بيرمي هنا — مفيش أي أثر
> await cancelOrder(...);
> await setMetafields(...);
> ```
>
> **② الفشل *بعد* الفعل الأساسي = `warning` مش `error` — ومعاه إجراء الإصلاح.**
> لما الحالة تتكتب وبعدين خطوة تكميلية تفشل، تسجيلها `error` بيعمل **حفرة
> مقفولة**: الصف بيقول فشل، الأوردر بيفضل في القايمة، وإعادة المحاولة بترجّع
> «انتقال غير مسموح» لأن الحالة اتحركت خلاص. الوحيد اللي بيصلّحها تدخل يدوي.
>
> ```js
> // ✅ كل فعل تكميلي في try/catch، والرسالة بتقول المطلوب مراجعته
> try {
>   const n = await cancelFulfillments(env, token, order.fulfillments);
>   actions.push(`fulfillmentCancel×${n}`);
> } catch (e) {
>   warnings.push(`الحالة اتكتبت، لكن إلغاء التنفيذ فشل (${e.message}) — الأوردر لسه Fulfilled، ألغيه يدويًا`);
> }
> ```
>
> **③ والفعل الأساسي نفسه يبقى idempotent لما يقدر.** أوردر ملغي على شوبيفاي
> أصلاً: تخطّى `orderCancel` واكتب الحالة + `warning`. من غير كده الصف
> بيفضل أحمر للأبد وإعادة المحاولة **مستحيلة**.
>
> **السابقة:** `Order-Status-Updater` — `#52732` (أوردر ملغي بحالة قديمة،
> اتصلّح باليد، والسبب الإلزامي ضاع) + حالتين تانيتين في السجل
> (`reverseDispose: Invalid disposition quantity` 18-08 ·
> `fulfillmentCreate: api_client does not have access` 14-08) — الاتنين
> **بعد** ما الحالة اتكتبت، والاتنين اتسجّلوا `error`.

**وفي Code Review Checklist أضِف:**

```
- [ ] **كل تحقق ممكن يتعمل قبل أول فعل لا رجعة فيه** — مفيش فحص قيمة أو
      صلاحية بعد `orderCancel`/`refundCreate`
- [ ] **الأفعال التكميلية بعد الفعل الأساسي في try/catch → `warning`** مش
      `error` — ورسالة التحذير بتقول **إيه** المطلوب مراجعته يدويًا
- [ ] **الفعل الأساسي idempotent لما يقدر** — إعادة المحاولة على أوردر
      اتنفّذ عليه نص العملية لازم تكمّل، مش ترفض
```

---

## ٧ — بند جديد: حارس السر الغايب 🟢

**المكان:** `Step 8 — Secrets & Variables`، تحت تحذير الـ Promote.

**الصياغة المقترحة:**

> 🔴 **حارس السر الغايب — إلزامي كأول سطر في الـ handler.**
>
> ```js
> if (!auth || auth !== `Bearer ${env.WORKER_SECRET}`)
>   return json({ error: 'Unauthorized' }, 401, request);
> ```
>
> السطر ده **مكسور أمنيًا** لو `WORKER_SECRET` غايب: القالب بينتج السلسلة
> الحرفية `"Bearer undefined"`، يعني أي طلب معاه الهيدر ده **بيعدّي**.
>
> والحالة دي مش نظرية: إضافة سر من غير Promote · سر اتمسح بالغلط · Worker
> شبح اتنشر باسم مختلف — كلها بتدّي `env.WORKER_SECRET === undefined`.
> الحالة اللي المفروض تبقى «كل حاجة 401» بتتحول لـ **«الحماية اتشالت»**،
> وعلى Worker كتابة ده معناه إلغاء أوردرات وتحريك مخزون.
>
> ```js
> // ✅ قبل فحص الـ auth بالظبط
> if (typeof env.WORKER_SECRET !== 'string' || !env.WORKER_SECRET.trim())
>   return json({ ok: false, error: 'WORKER_SECRET غير مضبوط على الـ Worker', step: 'env' }, 500, request);
> ```
>
> ⚠️ نفس المنطق موجود أصلاً في قالب التدوير في `ecommoda-constants` §6
> (`if (!ACCEPTED.length) return json(…, 500)`) — لكنه **مش** في القالب
> العادي، فالأدوات اللي مش في مجموعة سر مالهاش الحارس.

**وفي Checklist:**

```
- [ ] **حارس `WORKER_SECRET` الغايب قبل فحص الـ auth** — من غيره السر
      الناقص بيدّي `"Bearer undefined"` مقبول، مش 401
```

---

## ٨ — بند جديد: التوقيت يتحسب مايتكتبش 🟢

**المكان:** `Step 7 — D1 Logging`، أو قسم جديد صغير `§HELPERS::time`.
وله نظير في `ecommoda-html-builder` (Step 7 — Time Display).

**الصياغة المقترحة:**

> ### ⏰ توقيت القاهرة — يتحسب، مايتكتبش ثابت
>
> ```js
> // ❌ النمط الحالي في أدوات كتير
> const CAIRO_OFFSET_HOURS = 3;   // ⚠️ Egypt DST ends 29-10-2026 → change to 2
> ```
>
> التعليق ده **مش حماية** — هو تذكرة لحد يفتكر يعملها في ٢٨ أداة في يوم
> واحد. ولو ما اتعملتش، الأداة **مابتشتكيش**: كل وقت معروض بيغلط بساعة، وكل
> حد فلتر تاريخ بيزحف ساعة، **وأي تاريخ بيتكتب على مورد حقيقي بيبقى غلط**
> (`custom.pickup_date` بيتكتب من `cairoDate()` — أي شحنة بين ٢٣:٠٠ و٠٠:٠٠
> بتتسجّل بتاريخ اليوم اللي بعده).
>
> **Workers ومتصفحات الموظفين الاتنين بيدعموا `Intl` بالمناطق الزمنية
> الكاملة** — فمفيش سبب للثابت أصلاً:
>
> ```js
> const CAIRO_TZ = 'Africa/Cairo';
> const _cairoFmt = new Intl.DateTimeFormat('en-CA', {
>   timeZone: CAIRO_TZ, hourCycle: 'h23',
>   year: 'numeric', month: '2-digit', day: '2-digit',
>   hour: '2-digit', minute: '2-digit', second: '2-digit',
> });
> function cairoParts(d) {
>   const o = {};
>   for (const p of _cairoFmt.formatToParts(d)) if (p.type !== 'literal') o[p.type] = p.value;
>   if (o.hour === '24') o.hour = '00';        // حارس: بعض المحركات بترجّع 24
>   return o;
> }
> function cairoOffsetMinutes(d) {             // ١٨٠ صيفًا · ١٢٠ شتاءً
>   const p = cairoParts(d);
>   return Math.round((Date.UTC(+p.year, +p.month - 1, +p.day, +p.hour, +p.minute, +p.second) - d.getTime()) / 60000);
> }
> function cairoDate() { const p = cairoParts(new Date()); return `${p.year}-${p.month}-${p.day}`; }
>
> // حدود يوم تقويمي بالقاهرة → UTC — الإزاحة تتقاس عند **ظهر** اليوم
> // (أي تحويل توقيت بيحصل فجرًا، فالظهر بيدّي إزاحة اليوم الصحيحة)
> function cairoDayBoundsUTC(dateStr) {
>   const offMin = cairoOffsetMinutes(new Date(`${dateStr}T12:00:00.000Z`));
>   return {
>     start: new Date(Date.parse(`${dateStr}T00:00:00.000Z`) - offMin * 60000).toISOString(),
>     end:   new Date(Date.parse(`${dateStr}T23:59:59.999Z`) - offMin * 60000).toISOString(),
>   };
> }
> ```
>
> **⚠️ نطاق فوري:** التوقيت الصيفي في مصر بيخلص **29-10-2026**. دوّر على
> الثابت في الستاك كله وحوّله:
>
> ```bash
> grep -rn "CAIRO_OFFSET\|UTC+3\|3 \* 3600 \* 1000" --include="*.js" --include="*.html" .
> ```

**وفي Checklist:**

```
- [ ] **صفر إزاحة توقيت مكتوبة ثابت** — `Intl` بـ `timeZone: 'Africa/Cairo'`
      في الـ Worker وفي الواجهة، بنفس الدوال بالحرف
```

---

## ٩ — بند جديد: سقف صريح على عدد الـ IDs وتكلفة الاستعلام 🟢

**المكان:** `Step 5 — Shopify Integration`، تحت بلوك `nodes(ids:)`.

**الصياغة المقترحة:**

> ⚠️ **`nodes(ids:)` محتاج سقفين، مش واحد.**
>
> **① سقف على عدد الـ IDs.** الواجهة بتقسّم، بس ده مش حارس: شاشة فيها
> «تحديد الكل» على نتيجة بحث ممكن تبعت مئات الـ IDs في نداء واحد. شوبيفاي
> بترفض، والنتيجة **دفعة كاملة فاضية برسالة GraphQL مبهمة** — والأسوأ إن
> الواجهة بتفضل شغّالة بصفوف بلا حالة، وصف بلا حالة غالبًا **بيعدّي** فحوصات
> العميل (المجهول ≠ محظور).
>
> ```js
> const MAX_ORDER_IDS = 50;
> function tooManyIds(ids) {
>   return ids.length > MAX_ORDER_IDS
>     ? `عدد كبير: ${ids.length} — الحد ${MAX_ORDER_IDS}. قسّمهم على دفعات.` : null;
> }
> // في **كل** endpoint بياخد orderIds — مش في الرئيسي بس
> ```
>
> **② سقف على تكلفة الاستعلام — منفصل تمامًا.** بلوكات زي
> `returns { reverseFulfillmentOrders { lineItems { dispositions } } }` تكلفتها
> ~١٠٨ نقطة **للأوردر الواحد**. ٢٠ أوردر ≈ ٢١٦٠ نقطة — فوق سقف تكلفة
> الاستعلام الواحد، فالنداء بيترفض حتى لو عدد الـ IDs مقبول تمامًا.
>
> ```js
> const STATES_CHUNK         = 50;   // بدون البلوك الغالي
> const STATES_CHUNK_RETURNS = 5;    // معاه
> ```
>
> **③ وخلي التكلفة مرئية.** `data.extensions.cost.throttleStatus` بيرجع مع
> كل رد. اعرضه في `?action=diag` — من غيره الاقتراب من السقف **مابيبانش**
> غير بانفجار دفعة كاملة.

---

# القسم ج — `ecommoda-html-builder`

**الإصدار الحالي:** v6.6.0 → المقترح **v6.7.0** (٣ بنود Standards جديدة + ٣ تعديلات).

---

## ١٠ — Standards جديد: الإدخال يتقفل أثناء أي عملية دفعية 🟢

**رقم مقترح: #43.**

> | 43 | إدخال شغّال أثناء عملية دفعية | اللوب بيقرا مصفوفة العناصر **حية** (`items.slice(i, i+CHUNK)`) والمستخدم يقدر يضيف/يحذف في نفس الوقت — والإضافة اللي بتحط في الأول (`items = fresh.concat(items)`) **بتزحزح كل العناصر**: عنصر على حدود التقسيم **بيتبعت مرتين** (والتانية بترجع خطأ منطقي فتتحسب فشل وهي نجاح)، والعنصر اللي بعده **بيتخطّى خالص بلا أي رسالة**، وشريط التقدم بيرجع لورا | **لقطة + قفل**: `const batch = items.slice()` واللوب عليها هي، و`setUpdateLock(true)` بتقفل **كل** نقط التعديل (مربع السكان · الإضافة اليدوية · أزرار الحذف · مسح الكل · إضافة من مودال البحث) وبتغيّر الـ placeholder لسبب واضح. وحارس `if (isUpdating) return;` في كل دالة تعديل — القفل البصري لوحده مابيمنعش النداء البرمجي |

**⚠️ ملاحظة مهمة للتوثيق:** البند ده **خطير في الأدوات اللي بتشتغل بمسدس
سكانر تحديدًا** — الموظف بيبدأ يمسح الدفعة اللي بعدها وهو مستني، وده سلوك
طبيعي مش خطأ استخدام. (`Order-Status-Updater`: ٤٣٨ دفعة في ١٦ يوم = ٢٧
دفعة في اليوم لموظف واحد.)

---

## ١١ — Standards جديد: المدخل المجهول يترفض وقت الإدخال 🟢

**رقم مقترح: #44.**

> | 44 | مدخل السكانر اللي السيرفر ما عرفوش | الأداة بتقبل أي نص ≥ ٣ حروف كمعرّف، وبتدّي **صوت نجاح** و«✓ تمت الإضافة». ودالة الترطيب بتلفّ على **اللي رجع** بس (`Object.entries(data.statuses)`) فالـ ID اللي ما رجعش **مابيحصلهوش أي حاجة**: الصف بيظهر بـ `#—` بلا بادج ولا لون، والفحص بيعدّيه (المجهول ≠ محظور) — والموظف بيعرف إنه غلط **بعد** ما يضغط تنفيذ الدفعة | **قارن المطلوب بالراجع**: `ids.filter(id => !got.has(id))`. الناقص بياخد `unknown = true` → دالة الحظر بترجّع `true` → صف مميّز بصريًا (خلفية `--red-light` + شريط جانبي `inset 4px 0 0 var(--red)` + بادج «كود مش معروف») + **نافذة صريحة** فيها الأكواد وزرار «🗑 شيلهم من القايمة» + صوت خطأ + اهتزاز. والتنفيذ مقفول لحد ما يتشالوا |

**السياق:** في مخزن، مسح لاصقة غلط (باركود منتج · AWB شركة شحن · لاصقة قديمة)
حاجة **يومية**. `الأوردر غير موجود على Shopify` ظهر ٧ مرات في سجل أداة واحدة.

---

## ١٢ — Standards جديد: الكاش المشتق من إعدادات لازم TTL 🟢

**رقم مقترح: #45.**

> | 45 | كاش القوايم المسحوبة من تعريفات (choices · مناديب · أسباب) | `if (cache) return cache;` — كاش **بلا انتهاء** يعيش طول عمر التاب. والقوايم دي بتتعدّل من داشبورد شوبيفاي **بدون أي إشعار**: قايمة أسباب الإلغاء اتغيّرت **مرتين في ٤ أيام** (قياس 07-09-2026). الموظف اللي فاتح التاب من الصبح بيشوف قيم اتشالت، واختيار واحدة منها بيوصل لعطل حقيقي على السيرفر | **TTL قصير + تحديث إجباري عند نقطة طبيعية**: `if (!force && cache && Date.now() - cachedAt < 10*60*1000) return cache;` و`force = true` عند الخطوة اللي بتسبق الاستخدام (اختيار الحالة المستهدفة · فتح المودال). و**الـ Worker يتحقق كمان** قبل أي فعل لا رجعة فيه — الواجهة بتقلّل الاحتمال، الـ Worker بيقفل الباب (worker-builder Step 5A ⑩) |

---

## ١٣ — Standards #41: `.toast-warning` مفقودة فعليًا 🟢

**البند موجود ومكتوب صح** — بس فيه فجوة تنفيذية.

`Standards #41` بيقول «الأسماء المعتمدة الأربعة: success · error · warning ·
neutral»، وبيلزم الـ **CSS المشترك** بس (`shell/هب`) إنه يعرّف الأسماء
البديلة. النتيجة: أداة **مستقلة** ممكن تعدّي بكل الفحوص و`.toast-warning`
**مش معرّفة عندها خالص** — وأول استدعاء بالنوع الرسمي بيدّي **توست بلا
خلفية** (نص أبيض على شفاف)، فشل صامت.

**اتقاس في `Order-Status-Updater` v4.4.0:** صفر استخدام لـ `'warning'`، صفر
تعريف لـ `.toast-warning`، و**٢٧ استدعاء بـ `'error'`** كتير منهم تحذيرات
(«موجود بالفعل» · «مفيش أوردرات ناجحة للطباعة»). الأحمر لكل حاجة = الموظف
بيتعوّد يتجاهل الأحمر.

**الصياغة المقترحة — عدّل #41 وأضِف للفحص:**

> الأسماء المعتمدة الأربعة: `success` · `error` · `warning` · `neutral`.
> **الأربعة لازم يكونوا معرّفين في `:root`/CSS بتاع الأداة — حتى لو الأداة
> مش بتستخدمهم كلهم دلوقتي.** أي CSS مشترك يضيف `.toast-warn` و`.toast-info`
> كأسماء بديلة كمان.

**وفي Step 9:**

```bash
echo "── أنواع التوست الأربعة معرّفة (Standards #41) ──"
for t in success error warning neutral; do
  printf '.toast-%-9s %s (لازم ≥1)\n' "$t" "$(grep -c "\.toast-$t" "$FILE")"
done
echo "── تحذيرات متلبّسة أخطاء — راجعها بعينك ──"
grep -n "showToast(.*'error'" "$FILE" | grep -v "فشل\|تعذّر\|خطأ\|Unauthorized"
```

---

## ١٤ — Standards #29: الفحص الآلي مابيمسكش الرقم المتأخر 🟢

`Standards #29` عرّف `MIN_WORKER_VERSION` صح، وStep 9 بيفحص إنه **موجود**
وإن المقارنة رقمية — بس **مفيش أي فحص إن قيمته صح**.

**اتقاس:** `Order-Status-Updater` v4.4.0 كان فيه `MIN_WORKER_VERSION = '4.3.0'`
بينما الواجهة بتقرا `cap`/`total`/`truncated` من `get_logs_export` اللي
اتضافوا في الـ Worker **v4.4.0**. يعني الحارس نفسه كان **معطّل**: لو الـ
Worker رجع لـ 4.3.x، `data.truncated` بتبقى `undefined` → الشرط بيروح لفرع
`else` → **«✅ تم تصدير N صف» على ملف مقصوص** — بالظبط الفشل اللي
`Standards #30` اتكتب عشانه. وعدّى من كل فحوص Step 9.

**الصياغة المقترحة — أضِف لـ #29:**

> ⚠️ **الرقم بيتعتّق في صمت، والفحص الآلي مابيمسكهوش.**
> `MIN_WORKER_VERSION` رقم **يدوي بالكامل** — Step 9 بيتأكد إنه موجود
> وإن المقارنة رقمية، **مش** إنه مظبوط. والرقم المتأخر معناه إن الحارس
> شغّال وبيوافق على Worker **مش** قادر يخدم الصفحة.
>
> **قاعدة إلزامية في نفس التسليم:** أي حقل أو `action` جديد الصفحة بتقراه
> → ارفع `MIN_WORKER_VERSION` لنسخة الـ Worker اللي ضافته، **قبل** ما
> تسلّم. ولو التسليم فيه تغيير في الـ Worker والواجهة مع بعض، الرقمين
> بيتساووا غالبًا.
>
> **مراجعة يدوية إلزامية (مش قابلة للأتمتة):** افتح الفرق في `index.js`
> ودوّر على أي حقل مضاف في أي `return json({...})`، وبعدين `grep` على
> اسمه في الـ HTML. لو الواجهة بتقراه → الرقم لازم يبقى نسخة التسليم ده.

---

## ١٥ — Step 9A: البارسر مابيمسكش المستند المولّد 🟢

`Step 9A` (`css-check.js`) بيقارن **كل** `var(--x)` في الملف بكل التوكنز
المعرّفة في الملف — وده صح لصفحة الأداة، وبيغلط في حالة واحدة:

**المستند المولّد جوّه JS** (نافذة الطباعة · المانفيست · أي `window.open` +
`document.write`) هو **مستند منفصل** بـ `<style>` بتاعه ومن غير `:root`. أي
`var(--token)` جواه **فاضية وقت التشغيل** — والفحص بيعدّيها لأن التوكن معرّف
في الملف الأصلي.

**اتقاس:** مانفيست `Order-Status-Updater` كان فيه `color: var(--accent)` على
رقم الأوردر و`var(--text-secondary)` على ليبل الإجمالي — الاتنين فاضيين في
النافذة المطبوعة، والفحص عدّى ✅.

**الصياغة المقترحة — أضِف لـ Step 9A:**

> **فئة فشل رابعة: المستند المولّد.** الـ`<style>` اللي جوّه سلسلة نصية في
> JS (نافذة طباعة · مانفيست · تقرير منبثق) بيتحوّل لـ **مستند تاني** بلا
> `:root`. البارسر بيشوفه ضمن نفس الملف فبيعتبر توكنزه معرّفة — وهي فعليًا
> فاضية وقت التشغيل.
>
> **القاعدة:** أي CSS داخل مستند مولّد **يستخدم قيم صريحة** (`#2563eb`)
> مش توكنز — وده **الاستثناء الوحيد** المسموح لـ `Standards #35` (صفر hex
> خارج `:root`)، ويتكتب جنبه تعليق بالسبب.
>
> **الفحص:**
> ```bash
> # var(--x) جوّه سلسلة نصية بتتكتب في نافذة تانية
> grep -n "document.write\|window.open" "$FILE"
> awk '/window.open|document.write/,/<\/html>/' "$FILE" | grep -n "var(--"   # لازم فاضي
> ```
>
> **وحارس تشغيلي مصاحب (مش CSS):** `window.open()` بترجّع `null` لما
> المتصفح يمنع النافذة، والسطر اللي بعدها (`win.document.write`) بيرمي
> `TypeError` **مش متمسك في أي مكان** — يعني الطباعة بتفشل **بلا أي رسالة**
> والموظف فاكر إنها اشتغلت.
> ```js
> const win = window.open('', '_blank');
> if (!win) { showToast('المتصفح منع نافذة الطباعة — اسمح بالنوافذ المنبثقة', 'error', 8000); return; }
> ```

---

# ملحق — بنود ما اتغيّرتش (اتفحصت وطلعت سليمة)

عشان ما تتفتحش تاني:

| البند | النتيجة |
|---|---|
| `shopifyGQL` — عقد Step 5A | ✅ مطبَّق كامل (شبكة · status · JSON · `data.errors` · `data` فاضية · THROTTLED) |
| النتيجة ٣ حالات في الرد وفي `extra.result` | ✅ |
| `actions` بتتمرر من بره وبتتملي أول بأول | ✅ — والدليل الحي إن `#52732` سجّل `["orderCancel"]` رغم إنه رمى بعدها |
| `writeLog` بترجّع `logged:false` | ✅ |
| `assertEnv` + `requireLocationId` | ✅ |
| `tool`/`type` مسجّلين في `constants` §7 | ✅ `order_status` · `update`·`login`·`logout` |
| CORS Option B صارمة | ✅ |
| قيم S1/S2 مطابقة حرفيًا للتعريف الحي على شوبيفاي | ✅ اتقارنت 07-09-2026 (١٢ قيمة S1 · ٦ قيم S2) |
| أنواع الميتافيلد مطابقة للتعريف الحي | ✅ `pickup_date` = `date` · الباقي `single_line_text_field` |
| Standards #32 (قفل قوايم الـ multi-select) | ✅ الحارس موجود |
| Standards #30 (التصدير المقصوص) | ✅ مطبَّق — الفجوة كانت في #29 مش #30 |

---

## ملاحظة على نظام الإصدارات

كل التعديلات فوق تتصنّف وتتكتب في `references/CHANGELOG.md` بتاع كل مهارة
حسب `ecommoda-skill-versioning`:

| المهارة | من | لـ | السبب |
|---|---|---|---|
| `ecommoda-order-lifecycle` | v1.3.0 | **v1.4.0** | بندان 🔴 كاسران (٢ و٣) + انتقال جديد |
| `ecommoda-worker-builder` | v2.1.0 | **v2.2.0** | ٤ بنود مضافة، مفيش كاسر |
| `ecommoda-html-builder` | v6.6.0 | **v6.7.0** | ٣ Standards جديدة (#43 · #44 · #45) + ٣ تعديلات |

⚠️ وبعد الرفع: `Order-Status-Updater` هيبقى **متأخر عن البصمة** —
`CLAUDE.md` بتاعه شايل `order-lifecycle v1.3.0` وشايل القرارات دي محليًا.
حدّث جدول البصمة هناك في نفس التسليم اللي بيرفع المهارات.

---

آخر تحديث: 08-09-2026 — 01:20

</div>
